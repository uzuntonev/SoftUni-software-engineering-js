function solve(input) {
    let delimiter = input.pop();
    let output = input.join(delimiter);
    console.log(output) 
}

solve([
    'One',
    'Two',
    'Three',
    'Four',
    'Five',
    '-']);